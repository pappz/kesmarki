# Copy this file to .env.local (git-ignored) and fill in the value.
# The "webapp" broker password, used only when running in a plain browser
# (npm run serve / Firebase build). In the Android WebView the password
# comes from KesmarkiApp.getPassword() instead.
VUE_APP_MQTT_PASSWORD=
